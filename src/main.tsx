import React from 'react'
import ReactDOM from 'react-dom/client'
import RentalGedungApp from './app/rental-gedung/RentalGedungApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RentalGedungApp />
  </React.StrictMode>,
)